// export type JobScope = {
//   flowid: string;
//   runid: string;
//   stepid: string;
//   jobid: string;
//   capid: CapId;
//   toolid: string | null;
// };

// export type JobEvent<T extends JobEventType> = CloudEvent<T> & JobScope;

// job.mcp.submitted;

// job.started;

// cap.mcp.submitted;
// rm.cap.received;
// rm.cap.resolved;
// rm.cap.delayed;
// cap.mcp.queued;
// tool.mcp.invoked;

// job.mcp.submitted;
// rm.cap.received;
// rm.cap.resolved;
// rm.cap.delayed;
// job.mcp.queued;
// job.started;
// job.mcp.completed / failed;

// submitted;
// queued;
// finished;
// success / failure;
