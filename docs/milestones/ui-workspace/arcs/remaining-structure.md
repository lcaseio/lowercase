# UI Workspace Milestone — Arc: Remaining structure review (PR 49)

**Previous:** [Naming the shell — Workbench, Dock, FlowExplorer](./workbench-naming.md) (PRs 47–48) · **Next:** [Theme logic + visual polish](./theme-and-visual-polish.md) (PR 50)

Part of the [`MILESTONE.md`](../MILESTONE.md) PR log, split out to keep that doc scannable. Surfaced while wrapping up [`workbench-naming.md`](./workbench-naming.md)'s PR 48 — with `components/` fully reorganized (PRs 46–48) and holding only domain folders now, the obvious next question was what's still outside it. This arc is that look: `src/lib/`, `layout/`, `contexts/`, `hooks/`, `pages/`, `redux/`, and the package root — none of it was "Explorer stuff," so none of it was touched by PRs 1–48. Some of it predates the whole Explorer/Workbench rework and has just been sitting.

**Not a mechanical sweep the way PR 42's dead-code pass was — a real review, done a piece at a time, same methodology as the rest of this milestone: check real importers/usage directly, never assume from a file's name or age.** Discussion below grows as pieces get looked at; not everything under `## PR 49` was investigated in one sitting.

## PR 49 - Remaining structure review - merged (#332)

### Discussion

**Scope boundary, mechanical**: anything already reorganized by PRs 42–48 (`components/`, `pages/Workbench.tsx`, the `dock-*`/`explorer-*`-turned-`dock-*` redux files) is out of scope here — already done, not being revisited. In scope: everything else under `apps/web-app/`.

**Package root, reviewed 2026-08-20:**

- **`README.md` — real finding, in scope for this PR.** 100% unedited Vite scaffolding boilerplate (generic "React + TypeScript + Vite" template text, copy-pasted ESLint-expansion instructions) — never touched since the project was created, says nothing about this actually being a workflow-engine web app. Decided: worth fixing now, either deleted or replaced with something bare/basic and accurate — not a big writing project, just needs to stop being template filler.
- **`index.html`'s stale favicon/title — confirmed real, deliberately NOT this PR's job.** Still has the literal Vite-default favicon (`/vite.svg`, the actual stock logo file still sitting in `public/`) and `<title>web-app</title>`. Already tracked in `docs/todo.md` and already exactly PR 50's scope ("CSS visual polish pass — light + dark mode, page title/icon"). Confirmed genuinely load-bearing, not just a dev artifact: Vite treats `index.html` as the real build entry in both dev and production — `vite build` parses it, bundles the referenced `main.tsx` graph, and emits a real `index.html` into `dist/`, so the stale favicon/title actually ship. Leaving this alone here specifically to avoid redoing the discussion when PR 50 comes up.
- **`tailwind.config.ts` — flagged, not this PR's job either, likely PR 50's.** Not dead: `index.css` explicitly does `@config "../tailwind.config.ts"`, a real Tailwind v4 compatibility bridge for JS-based config that CSS-native `@theme` blocks don't yet cover. Likely partially redundant, though: the config's `darkMode: "class"` setting is very plausibly already superseded by `index.css`'s own `@custom-variant dark (&:where(.dark, .dark *));` — Tailwind v4's documented CSS-native replacement for exactly that same setting. Best guess: at some point getting class-based dark mode working needed both (matches the user's own memory of "a bit of a headache"), and once the CSS-native line started working the old JS setting never got removed. Not confirmed — would need actually pulling the `@config` line and checking dark mode still toggles correctly. **Decided (2026-08-20): this is PR 50's job, not PR 49's** — the user's upcoming light/dark visual-polish pass is "not just visual, but substantial on the light and dark mode" specifically, so this redundancy check belongs there.
- **`components.json` — checked, legitimate, no action.** shadcn/ui CLI config, genuinely in use.
- **`.DS_Store` — checked, no action needed.** Untracked, already covered by the root `.gitignore`.
- **`note.temp.ts` — the user's own file, not this PR's to touch.** Matches the established `*.temp.*` convention (same as `Runner-old.temp.tsx` earlier in this milestone) — gitignored, personal reference, being deleted by the user directly.
- **`eslint.config.js` — reviewed, clean, no action needed here.** Two very minor observations, neither a defect: `ecmaVersion: 2020` is lower than the TS configs' `target: "ES2022"`/`"ES2023"`, though this has caused zero real lint failures all milestone; and the stale `README.md` (above) literally suggests upgrading to `tseslint.configs.recommendedTypeChecked` for type-aware linting, an unexercised suggestion from the boilerplate, never adopted. **Decided (2026-08-20): if either is ever worth touching, it's part of PR 53's tooling pass, not this PR** — same reasoning as the `tsconfig.app.json`/`tsconfig.json` items below.
- **`tsconfig.app.json`'s `baseUrl` and `tsconfig.json`'s matching (and likely fully vestigial) `baseUrl`/`paths` block — real findings, moved out of this PR's scope.** `tsconfig.app.json`'s `baseUrl: "."` is confirmed dead: TS 5.9 resolves `paths` relative to the config file automatically once `baseUrl` is absent, and `moduleResolution: "bundler"` doesn't lean on it either — safe no-op removal whenever it happens. The root `tsconfig.json` has the identical `baseUrl`/`paths` block, but that file also has `"files": []` and only `references` (no `include` of its own) — meaning nothing is ever compiled directly under it, so that whole `compilerOptions` block looks entirely vestigial, not just deprecated. **Decided (2026-08-20): both belong to PR 53, not this PR** — the user wants TS-config tooling touched in the same pass as actually getting the build working, not piecemeal now.
- **`package.json` — reviewed, one dead dependency in scope for this PR, one major finding for PR 53.** `next-themes` has zero imports anywhere in `src/` — this app has always used its own hand-rolled theme context (`contexts/theme-provider.tsx`/`use-theme.ts`), so `next-themes` looks like a leftover from an earlier, replaced approach. Same "verified zero real usage" bar as `runner-slice.ts` — **added to this PR's scope for removal.** Checked other maybe-suspect dependencies too: `echarts`/`echarts-for-react` (used by `EventGraph.tsx`, `EvalScoreChart.tsx`), `radix-ui`, `sonner`, `jsonc-parser` — all confirmed genuinely used, not dead. **Real finding for PR 53, not this PR**: a `"build-old": "tsc -b && vite build"` script already exists, sitting right next to the `"build": "echo build"` stub — the real build command was never missing, just deliberately disabled and renamed. Once `@lcase/types`'s exports get fixed (PR 53's original finding), PR 53 is likely just swapping `build`/`build-old` back, not writing a new script.

**`redux/slices/runner-slice.ts` — real finding, in scope for this PR, ready to delete outright.** Checked every one of its 9 exported actions and 7 exported selectors against the rest of the codebase: zero real consumers anywhere, for any of them. One of its selectors, `selectFlows`, isn't just unused — it's a stray exact duplicate of `flows-slice.ts`'s own identical selector. The only two references to this file at all are `store.ts` importing `runnerSlice` to wire the reducer in and register it on `RootState`; nothing dispatches into it, nothing reads from it, and there's no raw `state.runner.*` access anywhere bypassing the selectors either. Confirms what PR 43 flagged as likely once its last real consumers (`FlowListItem.tsx`/`SimsListItem.tsx`) got pruned in PR 44/45 — this is that prediction landing. Fix: delete `runner-slice.ts` entirely, remove its `store.ts` wiring (`import`, the `runner: runnerSlice.reducer` line, and its `RootState` field).

**`contexts/`'s theme-logic question, carried over from PR 45's own investigation — resolved, not just narrowed.** Read `theme-provider.tsx`/`use-theme.ts` directly and traced `ThemeProvider`'s mounting in `main.tsx`: it's genuinely the one and only theme system, mounted at the true app root (wraps everything, above the Redux `Provider` even) — there's no second/legacy theme mechanism anywhere else in the app for it to conflict with. PR 45's question had a clean "no" answer. A few small, low-stakes things surfaced while reading it directly, though, real enough to be worth a genuine look: a `storageKey` still set to the literal unmodified shadcn-tutorial default (`"vite-ui-theme"`), a dead `if (context === undefined) throw` safety check in `useTheme()` (`createContext(initialState)` guarantees `useContext` never actually returns `undefined`), the resolved theme applied as a class to both `<html>` and `<body>` redundantly, and `tailwind.config.ts`'s likely-superseded `darkMode: "class"` setting (see this doc's own package-root section above). **Decided (2026-08-20): none of this is PR 49's job — moved to PR 50 instead**, which the user is widening from pure visual polish into a real investigation of the theme logic itself, not just a drive-by fix. See `MILESTONE.md`'s PR 50 entry for the full list.

**`layout/AppShell.tsx` — reviewed, one small unrelated finding (below), and one much bigger one: this turned into a real "what's the target shape of `src/`" discussion.** Otherwise clean — its nav array is already correctly updated to `/workbench` (PR 47). One dead branch: `isActive`'s ternary checks `item.to === "/"`, but no `navItems` entry points at root `/` anymore (only the `<Navigate>` redirects in `App.tsx` do) — that branch can never fire. Not theme-related, so not folded into PR 50 with the `contexts/` findings above; still an open, low-priority item for this PR whenever it's worth a one-line fix.

**A real target-shape decision, settled 2026-08-20, same spirit as the Workbench/Dock/FlowExplorer naming pass but one level up the tree — a new `app/` folder, consolidating `App.tsx` and `layout/AppShell.tsx`.** Surfaced because the two files are far more tightly coupled than their separate locations suggest: `AppShell.tsx`'s nav items have to mirror `App.tsx`'s `<Route path>`s by hand, yet one sits loose at `src/` root and the other alone in its own single-file `layout/` folder. Real, named precedent, not invented: [Bulletproof React](https://github.com/alan2207/bulletproof-react)'s `src/app/` convention — root provider composition, routes, and the top-level shell, distinct from `components/` (shared UI, already `components/workbench/`'s job here) and `pages/` (route-level screens, staying separate). `layout/` folder eliminated entirely — it only ever held this one file.

- **`layout/AppShell.tsx` → `app/RootLayout.tsx`, both file and export renamed.** `AppShell` never really described what the component does — traced its full history: created in the very first UI-shell commit (predating even PR 1's Explorer spike) as a collapsible shadcn `Sidebar`, simplified to today's fixed icon rail in PR 20, but _always_ just "top-level nav + `<Outlet/>` for whatever route is active," never anything shell-like beyond that. `RootLayout` is the actual, widely-recognized name for exactly this pattern — the literal convention Next.js App Router uses for a component that wraps every route with persistent chrome and an outlet. Same "borrow established real vocabulary" move as Workbench/Dock/FlowExplorer already made with VS Code's own terms, just a different source. Confirmed genuinely a layout, not just plumbing — it returns real JSX (the nav rail markup), not just route wiring.
- **`App.tsx` moves to `app/App.tsx`, kept as-is otherwise** — "App" isn't redundant with the `app/` folder the way a project-specific name would be; it's a near-universal fixed point across the entire React ecosystem (every scaffold names it this), not worth renaming away.
- **`TooltipProvider` moves out of `RootLayout`/`AppShell` entirely, into `main.tsx` alongside `ThemeProvider`/Redux `Provider`.** The component's own existing comment gives it away: _"stays even though the rail itself no longer needs it... `Rail.tsx` in both the `flow-graph-panel` and `event-graph-panel` directories rely on this app-root provider as their own Tooltip's Radix context ancestor."_ It's only in the nav component because it happened to be the nearest ancestor at the time, not because the nav rail itself needs it — genuinely an app-wide concern, belongs with the other app-wide providers. Confirmed safe: Radix's `TooltipProvider` doesn't need Router context, so moving it above `BrowserRouter` changes nothing.
- **Correction owed to `workbench-naming.md`'s own PR 47 discussion**: its original reasoning for choosing "Workbench" said "the plan is to grow what `AppShell` means" into it — that's stale now. What actually happened is `pages/Explorer.tsx` narrowed into `Workbench.tsx`, a sibling _route_ alongside Evals/System, all three hosted by this separate nav component (now `RootLayout`) — not the original plan. Worth fixing in that doc when this gets executed.
- **Ruled out, not a vestige worth investigating further**: the user's memory of an old flow-version-mode-nav mechanism ("the left nav was designed not to rerender, mode navigation unmounted the outlet") isn't in this file and never was — checked its full history, it's never contained anything beyond nav + outlet. That mechanism, if it worked the way remembered, almost certainly lived in the separate, now-deleted `FlowVersionModeNav.tsx`/`FlowVersionWorkspace.tsx` (a different, lower-level nav concept — mode-switching _within_ one flow version — pruned in PR 45).
- **Explicitly not designed for yet, flagged only**: the user's own framing — "eventually it will work different to where routing is not only done in this way for all links in this navigation... but it's fine for now." Today's `RootLayout` is a uniform `Link`-per-item nav; nothing about this rename commits to that staying true forever, and nothing here builds toward the eventual divergence either.

**Two more loose `src/`-root files, reviewed 2026-08-20, both resolved:**

- **`App.css` — delete entirely, in scope for this PR.** Unmodified Vite scaffolding CSS (`.logo`, the `logo-spin` keyframes, `.read-the-docs`, `.card`) — none of those classes exist anywhere in this app. Even its one rule that isn't an unused classname, `#root { max-width: 100%; margin: 0 auto; text-align: left; }`, is a genuine no-op given its own values (100% is the default max-width; `margin: 0 auto` centers nothing once max-width is 100%; `text-align: left` is the browser default). Every rule in the file does nothing. Fix: delete the file, remove `import "./App.css"` from `App.tsx`.
- **`sse-bootstrap.ts` — real, used code (the SSE event-stream connection, tied to PR 40), moves into `app/` alongside `RootLayout`/`App.tsx`.** It's a genuine React component (hooks, a JSX-returning function — today's `.ts` extension undersells that), rendered once at the app root in `main.tsx` next to `<Toaster/>` — a root-level bootstrapping concern, the same category `app/` already covers. Doesn't fit `redux/middleware/` (checked — that folder holds pure middleware modules, `route-event.ts`/`sse.ts`, no components). Fix: move to `app/SseBootstrap.tsx`, renaming the extension to reflect what it actually is.
- **Its hardcoded `"http://localhost:3000/events"` isn't a one-off oddity — checked, and it's a repo-wide pattern**: all 5 `redux/api/*.ts` files hardcode the identical `"http://localhost:3000/api/"` base URL too, 6 files total, zero env-var abstraction anywhere. Not this PR's job to fix (bigger than a file-location decision), but a real, concrete gap given the dual localhost-browser/Electron deployment target. **Decided (2026-08-20): routed to PR 53 instead of `docs/todo.md`** — the user's framing is that this is genuinely a build-config concern (Vite's `import.meta.env.VITE_*`, resolved per `.env`/build target), fitting naturally alongside PR 53's other build-activation work rather than a generic backlog item.

**`hooks/` — reviewed in full 2026-08-20, real policy question resolved, not just a file-by-file check.** The user's own framing: components already have plenty of colocated hooks (`flow-graph-panel/use-flow-graph-panel.ts`, `artifact-panel/use-artifact-panel.ts`, etc.) — so why do 8 more sit in a top-level `hooks/` folder, and is that even the right call? Checked every one of the 8 against its real consumers (not assumed from the name), applying the same rule as `components/workbench/shared/`'s own invariant one level up: verified multi-consumer stays generic, single-consumer moves to live with its one real caller.

- **Genuinely generic, confirmed by reading them directly (not just import-counting) — stay in `hooks/`**: `use-debounced-callback.ts` and `use-delayed-loading.ts` both import nothing but `react` — zero Workbench/flow/panel-specific code, could be lifted into any React app unchanged. Real precedent for keeping this distinction, not invented: `docs/todo.md`'s "package-tier taxonomy" discussion already carves out an analogous "pure, near-zero-dependency, broadly reusable" tier for the _backend_ (`packages/flow-analysis`, `packages/json-ref-binder`) specifically because domain-agnostic code deserves to live outside any one domain's tree — `hooks/`/`lib/` already play that same role for the frontend, just never named as a tier. The same source that justified `app/` (Bulletproof React) draws exactly this line too: top-level `hooks/`/`lib/` for genuinely shared code, colocated-with-the-feature for everything domain-specific.
- **Single-consumer, move to live with it**: `use-flow-analysis.ts` — only `flow-graph-panel/use-flow-graph-panel.ts` imports it → into `flow-graph-panel/`.
- **Matches `shared/flow-graph/`'s existing pattern (reused across a subset of the flow-graph-authoring trio, or by files already living there) — move in**: `use-flow-draft-analysis.ts` (used by `flow-authoring-panel` and `flow-authoring-preview-panel`, 2 of the 3 flow-graph-domain panels, not `flow-graph-panel` itself — same shape as `Rail.tsx`/`SidePanel.tsx` already there) and `use-step-run-info.ts` (6 consumers spanning both `shared/flow-graph/` itself — `FlowGraph.tsx`, `FlowStepNode.tsx`, `flow-step-accents.ts` — and `flow-graph-panel`-exclusive step-results content).
- **A real, different pairing, given a real new home — new `shared/events/`**: `use-flow-graph-replay.ts` and `use-run-events-with-status.ts` share the exact same 2-consumer pairing (`flow-graph-panel` + `event-graph-panel`), not the flow-graph-authoring trio `shared/flow-graph/` was scoped around. Read both directly: each takes a run's event history and derives UI-consumable state from it (replay-scrubbing state; fetched-events-plus-derived-status), and both internally depend on `use-step-run-info` to do it. "Events" as the domain name isn't a guess — this codebase already uses that exact vocabulary (`redux/slices/events-slice.ts`, `event-graph-panel/`, `event-payload-panel/`). `use-replay-clock.ts` (currently `use-flow-graph-replay.ts`'s own internal helper, never imported directly by anything else — confirmed via a full-repo grep after an initial pass missed it, since it's only ever reached via a relative `./use-replay-clock` import) follows automatically into the same folder.

Final placement once all of the above lands: `hooks/` keeps exactly 2 files (`use-debounced-callback.ts`, `use-delayed-loading.ts`); `flow-graph-panel/` gains `use-flow-analysis.ts`; `shared/flow-graph/` gains `use-flow-draft-analysis.ts` and `use-step-run-info.ts`; a new `shared/events/` holds `use-flow-graph-replay.ts`, `use-replay-clock.ts`, and `use-run-events-with-status.ts`.

**`lib/` — reviewed in full 2026-08-20 (`flow-graph-layout.ts` already got a targeted check during PR 48 and passed — see `workbench-naming.md`'s PR 48 entry). Same methodology as `hooks/`: read each file directly, check real importers, apply the same generic-vs-domain rule.**

- **`run-panel-state.types.ts` — dead, delete outright.** Zero real consumers anywhere, confirmed with an unrestricted repo-wide grep (not just the usual import-path pattern). Reading it explains why: all 3 exported types are prefixed `FlowVersionRun*` — leftover naming from the old `pages/flow-version/Run.tsx`-era page, deleted in PR 45. Same shape as `runner-slice.ts`.
- **`utils.ts` — stays in `lib/`, not a close call.** Almost certainly the `cn()` classname helper — used by essentially everything, including every `components/ui/*.tsx` shadcn primitive itself. The single most genuinely universal file in the app.
- **Single-domain, move to live with their consumer(s)**: `preview-text.ts` (3 consumers, all inside `flow-graph-panel/side-panel/step-results/` → moves into that exact folder); `detect-authored-format.ts` (2 consumers, both inside `artifact-authoring-panel/` → moves into that folder).
- **Single-consumer, colocate with the caller's own location**: `detect-file-format.ts` (1 consumer, `CreateArtifactDialog.tsx`, already in `shared/` → moves into `shared/` alongside it).
- **Broadly reused across an artifact-adjacent pair, joins `shared/` directly (no subfolder earned yet)**: `format-bytes.ts` (2 consumers: `CreateArtifactDialog.tsx` in `shared/`, and `artifact-panel/side-panel/MetadataTab.tsx` → moves into `shared/`, becoming a third bare artifact-related file there alongside `CreateArtifactDialog.tsx`/`artifact-title.ts`, matching the "subfolders are earned by real size" rule rather than presuming an `artifact/` subfolder is needed).
- **Broadly reused across three different panel families, matches `CodeEditor.tsx`/`MainPanelTypes.ts`'s own profile — moves into `shared/`**: `ref-resolution.ts` (10 consumers spanning `artifact-authoring-panel`, most of `flow-graph-panel/side-panel/step-results/*`, and `artifact-panel`). Checked its own imports: domain-aware (`FlowDefinition`, `Ref` from `@lcase/types`), not a pure generic utility like `utils.ts`, but reused broadly enough across panel families to match the exact shape that already justified `CodeEditor.tsx`/`MainPanelTypes.ts` living in `shared/`.

Final placement once all of the above lands: `lib/` keeps exactly 2 files (`utils.ts`, `flow-graph-layout.ts` — the latter already confirmed correctly placed during PR 48); `shared/` gains `detect-file-format.ts`, `format-bytes.ts`, and `ref-resolution.ts`; `flow-graph-panel/side-panel/step-results/` gains `preview-text.ts`; `artifact-authoring-panel/` gains `detect-authored-format.ts`; `run-panel-state.types.ts` is deleted.

**`pages/` — reviewed 2026-08-20, folder itself confirmed fine, one small real fix applied directly.** `pages/` as a convention needs no rename or relocation, unlike `layout/` — it's one of the most common React patterns for route-level screen components independent of framework, and it's explicitly one of the patterns Bulletproof React (already cited for `app/`) endorses too. All three files (`Evals.tsx`, `System.tsx`, `Workbench.tsx`) are genuinely current and used, nothing stale. One real inconsistency, fixed directly rather than left open: `Evals.tsx` was the one file still using old-style relative imports (`../components/evals/...`, `../redux/api/evals-api`, `../components/ui/input`) instead of the `@/` alias every other file this whole milestone uses, including its own siblings. Fixed to `@/`-rooted imports; `pnpm typecheck` confirmed clean.

**`redux/` — reviewed in full 2026-08-20, mostly a validation pass, one real move.**

- **All 5 `redux/api/*.ts` files confirmed genuinely used** (2–12 real importers each, checked directly, not assumed from the user's own read) — nothing dead.
- **Top-level `redux/`, parallel to `components/`/`hooks/`/`lib/`, confirmed as the right call, not a deviation worth second-guessing.** State that's genuinely cross-cutting (e.g. `flow-graph-panels-slice`, consumed across multiple different panel domains) belongs at this level rather than buried inside `components/` — the same shape Bulletproof React (already cited for `app/`/`pages/`) uses too.
- **`middleware/route-event.ts` confirmed intentional, left as-is.** Read directly: `routeEvent()`'s one real case (`"run.started"`) does nothing but return, with an honest `// no custom dispatching yet` comment — but it's genuinely wired into the store (`store.ts` imports and `.prepend()`s it as real middleware) and actually runs for every event batch. A real, executing extensibility point kept deliberately as a demonstration of what's possible to route to, not dead code to clean up.
- **`runner-slice.ts` — confirmed dead (see above), deleting.**
- **`dock-persistence.ts` moves to `components/workbench/dock/dock-persistence.ts`, correcting a small inconsistency from how PR 47 actually executed.** Not Redux logic itself (no `createSlice`, no actions/reducers — just `localStorage`/`sessionStorage` read-write functions that happen to import Redux slice _types_ for the snapshot shape), and its snapshot's _trigger_ is genuinely Dock's own lifecycle: dockview tears down and rebuilds panels on remount, which is the concrete reason this persistence layer exists at all (not incidentally Redux-shaped). `workbench-naming.md`'s own original PR 47 scope for "Dock" explicitly named _"layout persistence"_ as part of what Dock covers — PR 47's actual execution kept it in `redux/` based on reasoning that was really about keeping _slices_ consistent with every other panel slice, which never actually applied to this file (it was never a slice). The snapshot bundling every panel-kind's state alongside the dockview layout doesn't change this — the gating/trigger logic is still Dock's. `redux/slices/dock-tabs-slice.ts` stays in `redux/slices/` — it's a real slice, and the "every slice lives in `redux/slices/`" convention still holds for it.
- **`store.ts`/`typed-hooks.ts` — confirmed fine as-is, no changes.**

This closes out the full `src/` review for PR 49 — every folder outside `components/` has now been looked at directly.

**Full decided scope for this PR, as of the complete `src/` review (2026-08-20) — every folder outside `components/` has now been looked at directly, nothing left unreviewed**: `README.md` (delete or replace with something bare/basic), deleting `runner-slice.ts` and `run-panel-state.types.ts`, removing the dead `next-themes` dependency, deleting `App.css`, the new `app/` folder consolidation (`layout/AppShell.tsx` → `app/RootLayout.tsx`, `App.tsx` → `app/App.tsx`, `sse-bootstrap.ts` → `app/SseBootstrap.tsx`, `TooltipProvider` relocated to `main.tsx`, `layout/` eliminated), the `hooks/` redistribution (2 stay, 6 move into `flow-graph-panel/`, `shared/flow-graph/`, and a new `shared/events/`), the `lib/` redistribution (2 stay, 5 move into `shared/`, `flow-graph-panel/side-panel/step-results/`, and `artifact-authoring-panel/`), and `dock-persistence.ts` moving into `components/workbench/dock/` — the `isActive` dead branch rides along as part of the `app/` edit. Everything TS-config/tooling-shaped (`tsconfig.app.json`/`tsconfig.json`'s `baseUrl`, `eslint.config.js`'s minor observations, and the hardcoded-`localhost:3000` base-URL question across `sse-bootstrap.ts`/`redux/api/*.ts`) moved to PR 53 instead — the user wants tooling and build-activation touched together in one focused pass, not piecemeal here. Everything theme-logic-shaped (`contexts/theme-provider.tsx`'s findings, `tailwind.config.ts`'s dark-mode redundancy) moved to PR 50 instead — the user is widening that PR into a real investigation of the theme logic, not just visual polish, so a drive-by fix here would preempt that. `context/`'s old-vs-new conflict question is fully resolved (see above) — nothing left open there. `Evals.tsx`'s relative-import fix is already applied directly, not just planned. Nothing left open — ready to execute.

### What actually landed

Executed the full decided scope above, in one pass, 2026-08-20:

- **`README.md`** replaced (not deleted) — package name, one-line description, and the real `pnpm dev` command, pointing to the repo root README for actual project context. Decided against using `apps/cli/README.md` as precedent once the user flagged it as old, barebones code, not a model to follow.
- **`runner-slice.ts`** and **`run-panel-state.types.ts`** deleted outright; `store.ts`'s `runnerSlice` import/wiring/`RootState` field removed.
- **`next-themes`** removed from `package.json`, `pnpm install` run to update the lockfile.
- **`App.css`** deleted; its `import` removed from `App.tsx`.
- **New `app/` folder built**: `layout/AppShell.tsx` → `app/RootLayout.tsx` (component renamed `AppShell` → `RootLayout`, and the dead `isActive` ternary branch — `item.to === "/"` — simplified away to a plain `startsWith` check); `App.tsx` → `app/App.tsx` (its route imports switched to `@/`-absolute while moving); `sse-bootstrap.ts` → `app/SseBootstrap.tsx` (extension renamed to reflect the JSX it returns, its two relative imports promoted to `@/`-absolute since it moved a level deeper); `TooltipProvider` moved out of `RootLayout` into `main.tsx`, wrapping `SseBootstrap`/`Toaster`/`App` alongside `ThemeProvider`/the Redux `Provider`; `layout/` deleted, now empty.
- **`hooks/` redistributed** exactly as planned: `use-debounced-callback.ts`/`use-delayed-loading.ts` stayed; `use-flow-analysis.ts` → `flow-graph-panel/`; `use-flow-draft-analysis.ts` + `use-step-run-info.ts` → `shared/flow-graph/`; `use-flow-graph-replay.ts` + `use-replay-clock.ts` + `use-run-events-with-status.ts` → new `shared/events/`. Internal cross-imports between the split-apart files (`use-flow-draft-analysis.ts` → `use-flow-analysis.ts`, `use-flow-graph-replay.ts`/`use-run-events-with-status.ts` → `use-step-run-info.ts`) were promoted from relative to `@/`-absolute since their targets landed in different folders; `use-replay-clock.ts` stayed a relative import from `use-flow-graph-replay.ts` since both landed in `shared/events/` together.
- **`lib/` redistributed** exactly as planned: `utils.ts`/`flow-graph-layout.ts` stayed; `preview-text.ts` → `flow-graph-panel/side-panel/step-results/`; `detect-authored-format.ts` → `artifact-authoring-panel/`; `detect-file-format.ts`, `format-bytes.ts`, `ref-resolution.ts` → `shared/`.
- **`dock-persistence.ts`** → `components/workbench/dock/dock-persistence.ts`, its relative slice-type imports promoted to `@/`-absolute; both consumers (`store.ts`, `pages/Workbench.tsx`) updated.
- Every import site across `src/` and `tests/` updated to match (found by grepping each moved file's old path, not assumed) — `tests/` needed its own pass since the earlier `src/`-only grep missed it on the first sweep, caught immediately by the post-move typecheck.
- `workbench-naming.md`'s owed correction applied: its PR 47 section no longer claims "the plan is to grow what `AppShell` means" into Workbench.

**Verification**: root-level `pnpm typecheck` (25/25 packages), `pnpm lint` (26/26, `web-app`'s real ESLint config clean), `pnpm test` (`web-app`: 18 files/227 tests passing, including every test file whose import path moved) all clean. A repo-wide grep for every old path string (`hooks/use-*`, `lib/*`, `layout/AppShell`, `redux/dock-persistence`, `next-themes`, `runner-slice`, `run-panel-state`) came back empty. Dev server boot-checked: `vite` starts clean, `/`, `/src/app/App.tsx`, `/src/app/RootLayout.tsx`, and `/src/app/SseBootstrap.tsx` all transform and serve without error.
