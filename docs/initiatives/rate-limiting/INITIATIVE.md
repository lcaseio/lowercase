# Limiter: Real Rate Limiting

## Summary

`packages/limiter` currently only enforces concurrency limits per tool; add an actual rate limiter alongside it. Expected to be a small, contained addition — the package was built with this extension in mind.

Scaffolded now, work not yet started.
